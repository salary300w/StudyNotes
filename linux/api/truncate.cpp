/*
#include <unistd.h>
       #include <sys/types.h>

       int truncate(const char *path, off_t length);
        作用：缩减或扩展文件的尺寸到指定大小
        参数：
            path：文件路径
            length：需要文件最终变成的大小
        返回值：成功为0，失败为-1；
*/

